name: Sentinel CI/CD Test

on:
  push:
    branches:
      - main

jobs:
  test-auth:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Log in to Azure using service principal
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Echo success (simulate deployment)
      run: echo "✅ Successfully authenticated to Azure. No deployment executed."
